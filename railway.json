{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "sh -c 'if [ \"$RAILWAY_SERVICE_NAME\" = \"worker\" ]; then exec bash scripts/start_workers.sh; else exec uvicorn backend.main:app --host 0.0.0.0 --port $PORT; fi'",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
